[tool.poetry]
name = "ui-tests"
version = "0.1.0"
description = ""
authors = ["Smorodin_SA <ssmorodin@bk.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.14"
pytest = "^9.1.1"
pytest-playwright = "^0.8.0"
factory-boy = "^3.3.3"
pytest-cov = "^7.1.0"
pytest-django = "^4.12.0"
pytest-xdist = "^3.8.0"
allure-pytest = "^2.16.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
